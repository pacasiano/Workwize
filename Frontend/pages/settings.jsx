import Topbar from "../components/general/topbar"

export default function Settings() {

    return (
        <div>
            <Topbar setTitle={"Settings"} />
            <h1>Settings ng project, idk ano ba dapat, siguro yung name ng project, </h1>
            <input></input>
            <button>Submit</button>
        </div>
    )
}